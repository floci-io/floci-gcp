FLOCI_GCP_ENDPOINT=http://localhost:4588
FLOCI_GCP_PROJECT=test-project
PUBSUB_EMULATOR_HOST=localhost:4588
FIRESTORE_EMULATOR_HOST=localhost:4588
DATASTORE_EMULATOR_HOST=localhost:4588
STORAGE_EMULATOR_HOST=http://localhost:4588

# CTF operator root Bearer token (must match FLOCI_GCP_AUTH_ROOT_ACCESS_TOKEN on the emulator)
GOOGLE_OAUTH_ACCESS_TOKEN=fake-token-floci-gcp
FLOCI_GCP_AUTH_ROOT_ACCESS_TOKEN=fake-token-floci-gcp
CLOUDSDK_AUTH_ACCESS_TOKEN=fake-token-floci-gcp
